.venv/
.venv-cuda/
.venv-xpu/
__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/
release/
model_cache/
runtime/

# Native harness build output and the NVIDIA SDK it links against.
native/build/
native/_sdk/
native/bin/

# Never commit NVIDIA runtime binaries. The app locates the user's own copy.
*.dll
*.addon64

# ONNX depth models: generated by scripts/export_onnx.py and bundled into the
# release at build time. Too large for git; regenerate them instead.
dlss5_converter/assets/onnx/
*.onnx
onnx_models/

# Test images. Game promo art and screenshots are not ours to redistribute,
# and the app needs no sample data to run.
sample_image/
*.log

# Local test media, not for the repo (large, and not ours to redistribute)
sample_video/
*.mp4
*.mov
*.mkv
*.webm

# Private working notes, kept out of the fork.
TODO.md
